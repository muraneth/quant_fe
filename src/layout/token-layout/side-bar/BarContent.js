import Button from '@mui/material/Button';
import Box from '@mui/material/Box';
const BarContent = () => {
  return (
    <Box sx={{ width: '10px', bgcolor: 'background.deep' }}>
      <Button>Click Me</Button>
    </Box>
  );
};

export default BarContent;
